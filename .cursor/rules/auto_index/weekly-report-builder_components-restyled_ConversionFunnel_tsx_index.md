# weekly-report-builder/components-restyled/ConversionFunnel.tsx 函数索引

> 自动生成于 2026-04-23 | 总行数: 733 | 函数数: 6 | 语言: typescript
> **本文件由 code-indexer 脚本自动生成，严禁手动编辑。**

**巨型函数警告**: 本文件包含 1 个超过 200 行的函数，建议优先通过 `@section` 标记进行内部导航。

## 函数列表

> 定位方式：在源文件中 `grep -n "函数名"` 即可跳转，行号不在此列出（行号随代码变化而失效）。

| 函数名 | 类型 | 签名 | 备注 |
|--------|------|------|------|
| formatNumber | function | `formatNumber(n: number)` |  |
| formatRate | function | `formatRate(rate: number)` |  |
| FunnelTooltip | function | `FunnelTooltip({ data, visible }: TooltipProps)` |  |
| StepColumn | function | `StepColumn({ stepIndex, stepLabel, series, maxValue, unit, isBottleneck }: StepColumnProps)` |  |
| ConversionArrow | function | `ConversionArrow({ series, stepIndex }: ConversionArrowProps)` |  |
| ConversionFunnel | function | `ConversionFunnel({ title, description, series, unit = '人' }: ConversionFunnelProps)` | ⚠️ 巨型函数，见 @section 导航 |

## 巨型函数内部节点 (@section 标记)

### ConversionFunnel

> **缺少 @section 标记**：此巨型函数内部没有节点标记，建议添加以提升导航精度。

## 其他 @section 标记

| 节点标记 | 说明 |
|----------|------|
| `@section:conversion_funnel` | 漏斗转化组件 (Conversion Funnel) |
