# weekly-report-builder/components-restyled/UserSegmentationBubble.tsx 函数索引

> 自动生成于 2026-04-23 | 总行数: 975 | 函数数: 1 | 语言: typescript
> **本文件由 code-indexer 脚本自动生成，严禁手动编辑。**

**巨型函数警告**: 本文件包含 1 个超过 200 行的函数，建议优先通过 `@section` 标记进行内部导航。

## 函数列表

> 定位方式：在源文件中 `grep -n "函数名"` 即可跳转，行号不在此列出（行号随代码变化而失效）。

| 函数名 | 类型 | 签名 | 备注 |
|--------|------|------|------|
| CustomDot | function | `CustomDot({ cx = 0, cy = 0, r = 10, fill, payload, onClick }: CustomDotProps)` | ⚠️ 巨型函数，见 @section 导航 |

## 巨型函数内部节点 (@section 标记)

### CustomDot

> **缺少 @section 标记**：此巨型函数内部没有节点标记，建议添加以提升导航精度。

## 其他 @section 标记

| 节点标记 | 说明 |
|----------|------|
| `@section:user_segmentation_bubble` | 用户分层气泡图 (User Segmentation Bubble Chart) |
