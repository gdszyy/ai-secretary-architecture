# 历史任务归档说明

`archive/tasks_history/` 用于保存从原始单体仓库迁入的历史任务材料。该目录包含旧任务目录快照，目的是保留可追溯性，而不是作为当前系统的单一事实来源。

> 默认情况下，Agent 不应主动读取本目录；只有在需要追溯历史设计、核对旧交付物来源或恢复遗留实现时，才进入该目录。

当前归档策略采用 **完整快照保留 + 主上下文剥离**：将历史任务从主上下文中移除，但保留在归档区，避免前端代码、测试数据和过程性文档继续污染架构仓库的主阅读面。
